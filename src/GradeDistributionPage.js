import React from 'react';

const GradeDistributionPage = ({ course }) => {
  // Add code to fetch and display the grade distribution diagram
  return (
    <div>
      <h2>Grade Distribution for {course.name}</h2>
      {/* Add code to display the grade distribution diagram */}
    </div>
  );
};

export default GradeDistributionPage;
